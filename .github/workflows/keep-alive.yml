name: Keep Supabase Awake

on:
  schedule:
    # Runs every 5 days at midnight
    - cron: '0 0 */5 * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping the health endpoint
        run: curl -s https://your-portfolio-domain.com/api/health
